# WeatherNow
WeatherNow is an application that specifies the  weather attributes like temperature, AQI, humidity of a given location. Mostly python is used for the creation of WeatherNow. It is build using python web-framework "Django" and a minor part with frontend webstack like HTML, CSS, JavaScript. 
